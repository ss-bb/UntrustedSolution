//mhhhhh, i thinq it wasn't the solution they expected but I found it so funny

<SOLUTION>

function jump() {

map.placeObject(map.getPlayer().getX()+1,map.getPlayer().getY()+1,'computer');

}

<SOLUTION>

//just press Q as much time as you need :).
