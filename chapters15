//mhh. the name of the js file is interesting. 

function startLevel(map) {
    map.displayChapter('Chapter 3\nBetrayal');

    map.placePlayer(0, 0);

    // yoink!
    map.getPlayer().removeItem('theAlgorithm');

    map.defineObject('water', {
        'symbol': '░',
        'color': '#44f',
        'onCollision': function (player) {
        
            <SOLUTION>
            
            player.killedBy(BaguetteDomination); // or whatever undefined :)
            
            <SOLUTION>
        }
    });
